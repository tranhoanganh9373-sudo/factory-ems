# 性能验证报告 — 2026-04-25

> 由 `scripts/perf/dashboard-load.sh --report` 和 `scripts/perf/report-load.sh --report` 自动追加结果。
> 环境信息和观察结论由人工填写。

---

## 环境

| 项目 | 值 |
|---|---|
| Host | _填写：主机名 / OS / CPU 核数 / 内存_ |
| Java | _填写：`java -version` 输出_ |
| PostgreSQL | _填写：`psql --version` 输出_ |
| InfluxDB | _填写：`influx version` 输出_ |
| Docker Compose | _填写：`docker compose version` 输出_ |
| 测试时间 | 2026-04-25 |
| API 基址 | http://localhost:8888 |

---

## 数据集规模

| 项目 | 值 |
|---|---|
| 测点数 (meters) | 6（M-1 ~ M-6） |
| 时序数据时间窗 | 7 天（Phase J 种子数据） |
| 采样间隔 | 5 分钟 |
| InfluxDB 点数（估算） | 6 × 7 × 288 ≈ 12 096 点 |
| Rollup 表 | hourly_rollup + daily_rollup（Phase J 触发） |

---

## 看板压测结果

> 50 并发 × 100 请求 = 5000 次  
> 端点：`GET /api/v1/dashboard/kpi?range=TODAY` + `GET /api/v1/dashboard/realtime-series?range=LAST_24H`  
> 通过标准：**p95 < 1000 ms**

_运行后由 `dashboard-load.sh --report` 自动填写，结果追加在下方。_

| 指标 | 值 |
|---|---|
| p50 | — |
| p90 | — |
| p95 | — |
| p99 | — |
| mean | — |
| errorRate | — |
| 结论 | — |

---

## 报表导出结果

> 单接口 5 次迭代  
> 端点：`GET /api/v1/report/ad-hoc?from=...&to=...&granularity=HOUR`（近 7 天）  
> 预期行数：6 meters × 7d × 24h ≈ 1008 行  
> 通过标准：**median < 5000 ms**

_运行后由 `report-load.sh --report` 自动填写，结果追加在下方。_

| 指标 | 值 |
|---|---|
| min | — |
| median | — |
| max | — |
| 结论 | — |

---

## 观察到的瓶颈 / 后续优化

> 占位 — 由人工根据实测数据填写

- _例：KPI 查询未命中 rollup，走全量扫描 → 考虑增加 today 起始时间索引_
- _例：realtime-series 在高并发下触发 InfluxDB flux 查询队列堆积_
- _例：report ad-hoc HOUR 粒度下 join 6 meters 串行 → 考虑并行 flux query_

---

## 复现命令

```bash
# 前置：确保 docker compose 全栈运行，seed 数据已加载
# cd <project-root>

# L1 — 看板压测（需要 hey 或 node）
bash scripts/perf/dashboard-load.sh

# L1 — 看板压测并写入报告
bash scripts/perf/dashboard-load.sh --report

# L2 — 报表导出压测
bash scripts/perf/report-load.sh

# L2 — 报表导出压测并写入报告
bash scripts/perf/report-load.sh --report

# 覆盖默认参数示例
API=http://localhost:8888 CONCURRENCY=50 TOTAL=5000 bash scripts/perf/dashboard-load.sh --report
API=http://localhost:8888 ITERATIONS=5   bash scripts/perf/report-load.sh --report
```
