# 采集中断告警 · 业务规则

> **状态**：占位骨架。**Phase C**（异常 + ThresholdResolver + AlarmStateMachine 完成时）填充。
> 撰写依据：spec §3 检测逻辑、§14 状态机图

---

## 1. 状态机概览

（待 Phase C 填充：搬 spec §14.1 的 ASCII 图 + 文字描述）

## 2. 三种状态详解

### ACTIVE
（待 Phase C 填充：含义 / UI 表现 / 用户能做什么 / 后端行为）

### ACKED
（待 Phase C 填充）

### RESOLVED
（待 Phase C 填充）

## 3. 阈值解析规则

（待 Phase C 填充：设备覆盖优先 → 全局默认回落的具体逻辑，含部分覆盖示例）

例：设备 A 设置了 `silent_timeout_seconds=120` 但未设 `consecutive_fail_count`：
- 静默超时：120 秒（设备覆盖）
- 连续失败：3 次（沿用全局默认）

## 4. 抑制窗口（防抖）

（待 Phase C 填充：双重作用 — 防止 RESOLVED 后立即再触发 + 防止刚 ACTIVE 就被自动恢复）

## 5. 维护模式

（待 Phase C 填充：开启后系统行为、对现存告警的影响、关闭后会发生什么、备注字段建议）

## 6. 边界场景说明

（待 Phase C 填充：搬 spec §14.4 的边界场景表）

## 7. 用户应对场景手册

（待 Phase C 填充：列 5-7 个常见场景对应的状态变化）

例：
- **场景：设备真的坏了**
  - 系统：触发 ACTIVE
  - 用户：ADMIN 点确认 → ACKED；维修完毕设备恢复数据 → 5min 后自动 RESOLVED
- **场景：网络抖动 1 分钟**
  - 系统：可能不触发（取决于阈值）
  - 用户：无需操作
- **场景：计划停机维护 2 小时**
  - 系统：会触发，影响列表
  - 用户：建议维护前打开维护模式，维护后关闭

---

**Phase C 任务清单**（实施时执行）：
- [ ] 状态机图 + 三状态详解
- [ ] 阈值解析示例（部分覆盖）
- [ ] 抑制窗口的双重作用解释清楚
- [ ] 维护模式完整使用流程
- [ ] 5-7 个用户场景手册
- [ ] 删除本"任务清单"段
