{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp",
        "--isolated",
        "--headless"
      ],
      "description": "Browser automation via bundled Chromium (no extension required)"
    }
  }
}
